                  ********
                 *        *
                 *  TODO  *
                 *        *
                  ********

- Build up the data structure to allow the https query engine to log in inside
the system using a very simple administrative account.
- Improve the status coding naming.
- Add RSS
- Add email agent
- Improve the socket shutdown process (sometimes the server socket is not properly closed)
- Improve security
- Include monitor of Database engines like Teradata (excluded from the public version) and SQLServer (FreeTDS)
- I18n, Should be required. I do not know how to use it under C.
- Handling the Redirect response accordingly.

Bugs
- There is a memory leak in the thread management engine. 
- Set an upper bound to the timeout while connecting.
